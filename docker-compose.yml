version: "2"

services:
    backend:
        build:
            context: ./express-backend
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        container_name: back-end
        restart: always
